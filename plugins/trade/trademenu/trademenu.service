# DEPRECATED local template.
# Source of truth for the installed unit is:
#   installer/systemd/trade-web.service
# Installed as:
#   /etc/systemd/system/trade-web.service
#
# Do not use this file as a runtime unit. It no longer hardcodes /root/kam.
# After ./install.sh --trade, both Telegram /trade and the web UI load
# plugins from $HERMES_ROOT/plugins/trade with:
#   WorkingDirectory=$HERMES_ROOT
#   Environment=PYTHONPATH=$HERMES_ROOT
#   Environment=HERMES_HOME=$HERMES_HOME
#   ExecStart=$HERMES_ROOT/venv/bin/python -m plugins.trade.trademenu
# Bind remains 0.0.0.0:8001 (see plugins.trade.trademenu.config).
